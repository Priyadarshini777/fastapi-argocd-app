apiVersion: v1
kind: Service
metadata:
  name: fastapi-service
spec:
  type: LoadBalancer
  externalTrafficPolicy: Local     # ✅ required for proper LB health checks
  selector:
    app: fastapi-app
  ports:
  - name: http
    port: 80
    targetPort: 8000
